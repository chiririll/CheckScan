# CheckScan

Flutter Android app: scan a receipt QR, pick an adapter, store the receipt locally, then show history and simple stats.

## Run

```
flutter pub get
flutter test
flutter run
```

Android first. UI is Russian (`ru` only in v1).
