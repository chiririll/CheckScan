name: adapter_ru_fns
description: CheckScan adapter for Russian FNS fiscal QR codes.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  adapter_core:
    path: ../adapter_core
