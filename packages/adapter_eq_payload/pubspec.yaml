name: adapter_eq_payload
description: CheckScan adapter for QR codes that already contain eQ JSON.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  adapter_core:
    path: ../adapter_core
  crypto: ^3.0.6
